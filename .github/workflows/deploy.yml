name: Build and Deploy Static Site to Infomaniak

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Next.js static site
        run: npm run build
        
      - name: Verify build output
        run: |
          echo "=== Contenu du dossier out ==="
          ls -la out/
          echo "=== Fichiers HTML générés ==="
          find out/ -name "*.html" | head -5
          echo "=== CSS générés ==="
          find out/_next -name "*.css" | head -3

      - name: Deploy static files via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: out/
          server-dir: ${{ secrets.FTP_SERVER_DIR }}
          dangerous-clean-slate: true
          exclude: |
            **/.git*
            **/.git*/**
            **/.github*
            **/.github*/**
            **/node_modules/**
            **/src/**
            **/*.ts
            **/*.js.map

      - name: Deployment Success
        run: |
          echo "🎉 Déploiement statique terminé!"
          echo "🌐 Site: https://voyage.mathisinternicola.fr"
          echo "📁 Seulement les fichiers statiques du dossier out/ ont été déployés" 
